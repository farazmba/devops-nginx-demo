# Devops nginx demo

## Intro

This project help you install nginx on linuxand then show a demo page

## Steps

sudo-apt-get update
sudo-apt install nginx

sudo systemctl start nginx
sudo systemctl enable nginx
